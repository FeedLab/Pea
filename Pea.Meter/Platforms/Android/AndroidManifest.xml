<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Broad tablet support -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true" />

    <!-- Avoid <compatible-screens> to prevent hard filtering -->
    <!-- Ensures devices like Xiaomi Pad 6 and other mid-DPI tablets are included -->

<application
        android:icon="@mipmap/pea_raw"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:resizeableActivity="true">
    </application>

    <!-- Allow non-touchscreen devices: ChromeOS tablets, foldables in desktop mode -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Queries for web intents -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
