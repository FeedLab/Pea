<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:tabView="clr-namespace:Syncfusion.Maui.TabView;assembly=Syncfusion.Maui.TabView"
             xmlns:button="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
             xmlns:view="clr-namespace:Pea.Meter.View"
             xmlns:statistics="clr-namespace:Pea.Meter.View.Statistics"
             xmlns:solar="clr-namespace:Pea.Meter.View.Solar"
             xmlns:components="clr-namespace:Pea.Meter.Components"
             xmlns:properties="clr-namespace:Pea.Meter.Resources.Strings"
             x:Class="Pea.Meter.MainPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Resources>
        <Style TargetType="button:SfButton" x:Key="IconButtonStyle">
            <Setter Property="Margin" Value="2" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="EnableRippleEffect" Value="True" />
            <Setter Property="ShowIcon" Value="True" />
            <Setter Property="ImageSize" Value="22" />
            <Setter Property="ImageAlignment" Value="Default" />
            <Setter Property="TextColor" Value="#0F172A" />
            <Setter Property="Stroke" Value="#2B3C56" />
            <Setter Property="StrokeThickness" Value="0" />
            <Setter Property="Background" Value="#EEF2F7" />
            <Setter Property="VerticalOptions" Value="Center" />
            <Setter Property="HorizontalOptions" Value="Center" />
        </Style>

        <Style x:Key="IconButtonWithOpacityStyle" TargetType="button:SfButton"
               BasedOn="{StaticResource IconButtonStyle}">
            <Style.Triggers>
                <Trigger TargetType="button:SfButton" Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.25" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Icon resources -->
        <FontImageSource x:Key="AddMeterIcon" FontFamily="FontSolid" Glyph="&#xe541;" Color="DarkGreen" Size="28" />
        <FontImageSource x:Key="RemoveMeterIcon" FontFamily="FontSolid" Glyph="&#xe540;" Color="DarkRed" Size="28" />
    </ContentPage.Resources>


    <Grid
        Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Image
            Grid.Row="0"
            Grid.RowSpan="2"
            Source="solar_battery_meter.png" Aspect="Fill" Opacity="0.3" />

        <Grid
            Grid.Row="0"
            Margin="0, 6, 0, 18"
            HorizontalOptions="Fill"
            VerticalOptions="Start">
            <!--<Image
                Margin="12"
                Source="pea_amp.png"
                Aspect="AspectFit"
                HeightRequest="70"
                HorizontalOptions="Start"
                VerticalOptions="Start" />-->

            <view:CustomerInfoView
                Margin="20,0,0,0"
                HorizontalOptions="Start"
                VerticalOptions="Center" />

            <Border
                IsVisible="{Binding IsCustomerProfileVisible}"
                Margin="0"
                BackgroundColor="White"
                Stroke="#00A5A5"
                StrokeThickness="1"
                HorizontalOptions="Center"
                VerticalOptions="Center" 
                Padding="0">
                <Border.Shadow>
                    <StaticResource Key="PanelShadow" />
                </Border.Shadow>
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="3" />
                </Border.StrokeShape>
                <Image
                    Margin="0,0,0,0"
                    Source="pea_amr_banner_light_small.png"
                    Aspect="AspectFit"
                    HeightRequest="70"
                    HorizontalOptions="Center"
                    VerticalOptions="Center">
                </Image>
            </Border>

            <components:ToolbarComponent
                Margin="0,-15,20,0"
                HorizontalOptions="End"
                VerticalOptions="Center" />
        </Grid>

        <tabView:SfTabView
            Grid.Row="1"
            x:Name="TabView"
            Margin="0,0"
            EnableSwiping="False"
            TabBarPlacement="Bottom"
            TabWidthMode="SizeToContent"
            TabBarBackground="#CC000000"
            IndicatorBackground="#4CAF50">
            <tabView:SfTabView.Items>
                <tabView:SfTabItem Header="{x:Static properties:AppResources.PEA}" x:Name="Pea" TextColor="White"
                                   FontSize="14">
                    <tabView:SfTabItem.Content>
                        <Grid BackgroundColor="Transparent">
                            <view:HomeView />
                        </Grid>
                    </tabView:SfTabItem.Content>
                </tabView:SfTabItem>

                <tabView:SfTabItem Header="{x:Static properties:AppResources.Info}" x:Name="Info" TextColor="White"
                                   FontSize="14">
                    <tabView:SfTabItem.Content>
                        <Grid BackgroundColor="Transparent">
                            <view:InfoView />
                        </Grid>
                    </tabView:SfTabItem.Content>
                </tabView:SfTabItem>

                <tabView:SfTabItem Header="{x:Static properties:AppResources.Statistics}" x:Name="TabData"
                                   TextColor="White" FontSize="14">
                    <tabView:SfTabItem.Content>
                        <Grid BackgroundColor="Transparent">
                            <statistics:StatisticsView />
                        </Grid>
                    </tabView:SfTabItem.Content>
                </tabView:SfTabItem>

                <tabView:SfTabItem Header="{x:Static properties:AppResources.TariffCalculation}" x:Name="TouVsFlatRate"
                                   TextColor="White"
                                   FontSize="14">
                    <tabView:SfTabItem.Content>
                        <view:TouVsFlatRateView />
                    </tabView:SfTabItem.Content>
                </tabView:SfTabItem>

                <tabView:SfTabItem Header="{x:Static properties:AppResources.SolarSystemCalculationExperimental}"
                                   x:Name="SolarSystemSizing" TextColor="White"
                                   FontSize="14">
                    <tabView:SfTabItem.Content>
                        <solar:SolarSystemSizingView />
                    </tabView:SfTabItem.Content>
                </tabView:SfTabItem>

                <tabView:SfTabItem Header="{x:Static properties:AppResources.CustomerProfile}"
                                   x:Name="TabCustomerProfile" TextColor="White"
                                   FontSize="14">
                    <tabView:SfTabItem.Content>
                        <view:CustomerProfileView />
                    </tabView:SfTabItem.Content>
                </tabView:SfTabItem>

            </tabView:SfTabView.Items>
        </tabView:SfTabView>

    </Grid>

</ContentPage>